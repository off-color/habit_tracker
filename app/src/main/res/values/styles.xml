<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="editFormElement">
        <item name="android:layout_margin">@dimen/editFormLayoutMargin</item>
    </style>
    <style name="labelElement">
        <item name="android:paddingStart">@dimen/editFormPadding</item>
        <item name="android:paddingEnd">@dimen/editFormPadding</item>
    </style>
    <style name="habitTextView">
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>
    <style name="habitElement">
        <item name="android:paddingTop">@dimen/habitElementPadding</item>
        <item name="android:paddingBottom">@dimen/habitElementPadding</item>
    </style>
    <style name="habitEdgeElement" parent="habitElement">
        <item name="android:paddingStart">@dimen/habitElementPadding</item>
        <item name="android:paddingEnd">@dimen/habitElementPadding</item>
    </style>

</resources>
